import { NumLetrasPipe } from './num-letras.pipe';

describe('NumLetrasPipe', () => {
  it('create an instance', () => {
    const pipe = new NumLetrasPipe();
    expect(pipe).toBeTruthy();
  });
});
